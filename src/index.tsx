import React from 'react';
import ReactDOM from 'react-dom';


const App: React.FC = () => {
  return (
    <div>
      <h1>CST-323 Activity 1</h1>
    </div>
  );
};

// Rendering the App component to the DOM
ReactDOM.render(<App />, document.getElementById('root'));
